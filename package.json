{
  "name": "pouchdb-checksum",
  "version": "1.0.0",
  "description": "Generate a checksum for the data store in your database.",
  "main": "src/plugin.js",
  "scripts": {
    "jshint": "jshint src/*.js test/*.js *.json",
    "test": "npm run jshint && npm run test-unit",
    "test-unit": "mocha test/*.spec.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "jshint": "^2.9.5",
    "mocha": "^5.2.0",
    "pouchdb": "^7.0.0",
    "pouchdb-adapter-memory": "^7.0.0"
  }
}
